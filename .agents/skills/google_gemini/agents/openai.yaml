interface:
  display_name: "Google Gemini"
  short_description: "Repo-specific patterns and workflows for google_gemini"
  default_prompt: "Use the google_gemini repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true